# neural-whoop — State

> Generated by `tools/hypergraph.py render` from the state-graph export.
> Do not hand-edit — run the hypergraph-reconcile skill instead.

Reconciled through `weathered-hall-8765`, `still-truth-9599`, `fragrant-hill-8742`, `mute-block-8299`, `jolly-hat-7394`, `bitter-frog-1235`, `bitter-violet-6954`, `orange-pond-7208`, `patient-dew-6473`, `snowy-boat-4105`, `weathered-wildflower-1251`, `snowy-rice-0635`, `late-mud-4665`, `blue-field-6813`, `small-unit-3590`, `mute-pond-0406`, `wild-bird-1554`, `hidden-moon-2828`, `muddy-mouse-2952`, `still-bar-6348`, `fragrant-dream-2868`, `bold-shadow-8014`, `long-sea-0577`, `wild-waterfall-3546`, `floral-bird-7008`, `curly-dew-8661`, `late-bird-6048`, `holy-art-9938`, `divine-heart-1498`, `broken-tree-7316`, `wandering-water-2720` at 2026-08-09T18:50:00+00:00.

## Frontier

- [broken] **Hover deployment: the 1.0 m setpoint is outside the sensor's trusted band, and Desk-Hover has never been flown** (`lucky-lodge-5696`) — The shipped policy is `hover_tof_air65_w128u15`, chosen by the user at the second
- [broken] **Measurement and documentation integrity: a metric that could only return one value, and several published claims that are now false** (`autumn-bell-7061`) — This node exists because the project's own record found these [rec: shy-butterfly-3991], not because an
- [broken] **The real-drone deploy path: pilot flight engine, MSP bridge, and the radio link** (`modest-raven-7153`) — The path [rec: rapid-meadow-0957] is: policy on the host, through `neural_whoop.pilot` (a stdlib-only,
- [blocked] **Honest camera-only perception is blocked on an upstream Blackwell bug and on hardware that has not been integrated** (`vast-dune-7535`) — Perception is deliberately render-free in training: an `OracleEstimator` supplies the
- [blocked] **Onboard compute is measured and comfortable; nothing has been ordered** (`northern-rain-9996`) — The compute question is settled and it was settled by measurement, not projection.
- [open] **A pilot-side ToF zero-offset calibration does not exist, and it is the named blocker for a real Desk-Hover flight** (`rapid-hill-4130`) — The sensor itself is healthy: static noise floor **23.9 mm mean, sigma 2.4 mm** over
- [open] **Agility: reward-shaped flip discovery is refuted; tracking a hand-authored reference works for two maneuvers of three** (`loyal-wood-6029`) — Two approaches to the same capability [rec: calm-fog-9257], and the second exists because the first
- [open] **Gate-racing lap time: five levers exhausted against a standing control limit** (`autumn-stream-8410`) — `gate_race` is the locked first beachhead — single-drone time-optimal gate racing,
- [open] **The ESP-NOW link is built and flying, its own bench gates are unmet, and its stated premise was refuted** (`forest-timber-8727`) — Built end to end: an `espnow_dongle` firmware (USB CDC to ESP-NOW with MSP

## Architecture

- **neural-whoop — state** (`dusty-pine-0511`)
  - [working] **Batched environment and the task registry** (`cold-pebble-7468`) — `MultiAgentDroneEnv` (`src/neural_whoop/envs/base.py`) flattens `(n_envs, n_agents)`
    - [open] **Gate-racing lap time: five levers exhausted against a standing control limit** (`autumn-stream-8410`) — `gate_race` is the locked first beachhead — single-drone time-optimal gate racing,
    - [open] **Agility: reward-shaped flip discovery is refuted; tracking a hand-authored reference works for two maneuvers of three** (`loyal-wood-6029`) — Two approaches to the same capability [rec: calm-fog-9257], and the second exists because the first
    - [working] **Swarm: own-slot formation scales flat to 24 drones, shared-track racing caps at three** (`peaceful-mist-1317`) — Both swarm tasks exercise the `n_agents > 1` path with no env change — collisions and
    - [working] **Perception-in-the-loop following: the EMA precision filter is the reusable primitive** (`strong-spire-1133`) — Training feeds the policy a ground-truth body-frame target vector from an
  - [working] **The policy/env contract: obs-v4, act-v2 CTBR, and the domain-randomisation seam** (`long-mountain-5811`) — The contract is the sim2real seam and is deliberately simulator-independent — pure
    - [blocked] **Honest camera-only perception is blocked on an upstream Blackwell bug and on hardware that has not been integrated** (`vast-dune-7535`) — Perception is deliberately render-free in training: an `OracleEstimator` supplies the
  - [working] **Training, evaluation and the export path to a deployable policy** (`loyal-grove-4659`) — `training/ppo.py` is a torch-native PPO over the batched env (GAE with time-limit
  - [working] **Simulation substrate: the vendored DiffAero dynamics core** (`wise-trail-6304`) — The simulator is a vendored fork of DiffAero (BSD-3) under `third_party/diffaero`,
  - [broken] **Measurement and documentation integrity: a metric that could only return one value, and several published claims that are now false** (`autumn-bell-7061`) — This node exists because the project's own record found these [rec: shy-butterfly-3991], not because an
  - [working] **The visual observability seam: replay schema, Studio, and the in-repo video capturer** (`lawful-nest-5138`) — `viz/replay.py` [rec: square-smoke-0918] is a versioned, self-describing replay schema
  - [broken] **The real-drone deploy path: pilot flight engine, MSP bridge, and the radio link** (`modest-raven-7153`) — The path [rec: rapid-meadow-0957] is: policy on the host, through `neural_whoop.pilot` (a stdlib-only,
    - [open] **The ESP-NOW link is built and flying, its own bench gates are unmet, and its stated premise was refuted** (`forest-timber-8727`) — Built end to end: an `espnow_dongle` firmware (USB CDC to ESP-NOW with MSP
    - [broken] **Hover deployment: the 1.0 m setpoint is outside the sensor's trusted band, and Desk-Hover has never been flown** (`lucky-lodge-5696`) — The shipped policy is `hover_tof_air65_w128u15`, chosen by the user at the second
    - [blocked] **Onboard compute is measured and comfortable; nothing has been ordered** (`northern-rain-9996`) — The compute question is settled and it was settled by measurement, not projection.
    - [open] **A pilot-side ToF zero-offset calibration does not exist, and it is the named blocker for a real Desk-Hover flight** (`rapid-hill-4130`) — The sensor itself is healthy: static noise floor **23.9 mm mean, sigma 2.4 mm** over
